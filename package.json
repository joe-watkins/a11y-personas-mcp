{
  "name": "a11y-personas-mcp",
  "version": "1.0.0",
  "description": "MCP server providing accessibility personas for inclusive design and development",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "build": "node scripts/build-data.js",
    "update-personas": "git submodule update --remote --merge && npm run build",
    "postinstall": "git submodule update --init --recursive"
  }
}